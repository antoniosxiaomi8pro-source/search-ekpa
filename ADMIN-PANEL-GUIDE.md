# Admin Panel — Οδηγός χρήσης (Taxonomy Editor)

Το `public/admin-taxonomy.html` επιτρέπει να επεξεργάζεσαι το `concepts.json`
(την ταξινομία θεμάτων που χρησιμοποιεί η αναζήτηση για query expansion — π.χ. να
συνδέσεις τη λέξη "AI" με "τεχνητή νοημοσύνη", "machine learning" κλπ) **χωρίς να
χρειάζεται πρόσβαση στον server ή νέο deploy**.

---

## 1) Ρύθμιση (μία φορά)

Στο `.env` του server, όρισε ένα μυστικό token:
```
ADMIN_TOKEN=<κάτι-μεγάλο-και-τυχαίο>
```
Πρόταση για να το δημιουργήσεις:
```bash
openssl rand -hex 24
```
Αν το `ADMIN_TOKEN` **λείπει** από το `.env`, το admin panel είναι εντελώς
κλειδωμένο — γυρνάει `503` σε οποιαδήποτε προσπάθεια εγγραφής (fail-closed by
design, όχι κατά λάθος ανοιχτό).

Restart τον server μετά την προσθήκη (`pm2 restart ekpa-smart-finder`).

## 2) Πρόσβαση

Άνοιξε στον browser: `https://το-backend-url/admin-taxonomy.html`

> ⚠️ Αυτή η σελίδα **δεν** έχει δικό της login/password οθόνη μορφής "sign in" —
> λειτουργεί με το ίδιο το `ADMIN_TOKEN` σαν κλειδί. Οποιοσδήποτε γνωρίζει το URL
> ΚΑΙ το token μπορεί να κάνει αλλαγές. Μοιράσου το token μόνο με τους 1-2
> ανθρώπους που θα διαχειρίζονται πραγματικά το taxonomy, όχι ευρέως.

## 3) Χρήση

1. Βάλε το `ADMIN_TOKEN` στο πεδίο "unlock" της σελίδας
2. Θα φορτωθεί το τρέχον `concepts.json` σε επεξεργάσιμη μορφή
3. Πρόσθεσε/αφαίρεσε concepts ή terms
4. **Save** — γράφεται στο `concepts.json` μέσα στο `DATA_DIR` του server **και**
   ενημερώνεται αμέσως στη μνήμη (χωρίς restart, χωρίς redeploy). Χωρίς `DATA_DIR`
   γράφεται στο `public/concepts.json` — **σε Railway αυτό χάνεται στο επόμενο deploy**,
   δες `DEPLOY-Railway.md` §2.

## 4) Τι να προσέξεις

- Κάθε concept χρειάζεται τουλάχιστον 1 term, μέχρι 100 terms
- Κάθε term μέχρι 200 χαρακτήρες
- Ο server κάνει server-side validation πριν γράψει το αρχείο — αν η μορφή είναι
  λάθος, θα δεις μήνυμα σφάλματος και τίποτα δεν θα αποθηκευτεί (δεν μπορεί να
  "σπάσει" το αρχείο μισοτελειωμένο)
- Rate limited στα `ADMIN_RATE_LIMIT_PER_MIN` requests/λεπτό (default: 10) —
  σκόπιμα χαμηλό, αφού ένας άνθρωπος δεν χρειάζεται παραπάνω από λίγες
  αποθηκεύσεις ανά session

## 5) Πότε να το χρησιμοποιήσεις

Καλή πρακτική: χρησιμοποίησε το report "zero-result searches" (τώρα διαθέσιμο και
απευθείας εδώ, βλ. §6 παρακάτω, όχι μόνο μέσω GA4) για να δεις τι έψαξαν οι
χρήστες χωρίς αποτέλεσμα, και μετά πρόσθεσε αντίστοιχα concepts/terms εδώ ώστε η
επόμενη φορά να βρίσκουν κάτι σχετικό.

## 6) Νέο: Analytics tab (📊)

Δίπλα στο taxonomy, υπάρχει και ένα δεύτερο tab **📊 Analytics** (ίδιο token,
ίδιο unlock). Δείχνει τρεις πίνακες, ενημερωμένους κάθε φορά που πατάς
"🔄 Ανανέωση":

1. **Πιο συχνές αναζητήσεις** — τι ψάχνουν πραγματικά οι χρήστες
2. **Αναζητήσεις χωρίς αποτέλεσμα** — άμεσα candidates για νέα concepts (πρόσθεσέ
   τα στο Taxonomy tab)
3. **Πιο δημοφιλή προγράμματα βάσει κλικ** — ποια αποτελέσματα κλικάρονται τελικά

**Σημαντικό:** αυτό είναι **Φάση 1 — μόνο ορατότητα**. Τα κλικ/αναζητήσεις
καταγράφονται αλλά **δεν** επηρεάζουν ακόμα το πραγματικό ranking του
`/api/search`. Δες `HANDOVER-NOTES.md` για το σχέδιο μιας πιθανής Φάσης 2
(popularity boost στο ranking).

Τα δεδομένα αποθηκεύονται σε `analytics.json` μέσα στο `DATA_DIR` (ή
`server/analytics.json` αν δεν έχει οριστεί) — δεν είναι στο git, με όριο 2000
διαφορετικά queries (`ANALYTICS_MAX_QUERIES`). Όταν γεμίσει, αφαιρούνται πρώτα τα
λιγότερο συχνά και πιο παλιά queries.

**Πώς μετριέται μια αναζήτηση:** η live αναζήτηση στέλνει αίτημα σχεδόν σε κάθε
πλήκτρο («ψ», «ψυ», «ψυχ»…). Αυτά ενώνονται: μετράει μόνο η τελική λέξη, όταν ο χρήστης
σταματήσει να γράφει (~4 δευτ.), κάνει κλικ σε αποτέλεσμα ή ξεκινήσει άλλη αναζήτηση.
Γι' αυτό μια αναζήτηση μπορεί να εμφανιστεί στον πίνακα με λίγα δευτερόλεπτα
καθυστέρηση.

