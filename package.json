{
  "name": "ekpa-smart-finder-backend",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for EKPA Smart Finder: keeps the LLM API key server-side, does retrieval, calls the chosen LLM provider.",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
