# API Key setup (Anthropic)

Το backend χρειάζεται ένα API key από τον LLM provider που θα χρησιμοποιήσετε, για
να λειτουργήσει το `/api/chat` (ο AI βοηθός). Χωρίς αυτό, η υπόλοιπη εφαρμογή
(αναζήτηση, φιλτράρισμα, admin panel) δουλεύει κανονικά — μόνο το chat χρειάζεται
key.

Ο κώδικας υποστηρίζει ήδη 3 providers (`LLM_PROVIDER` στο `.env`): `anthropic`,
`openai`, `gemini`. Παρακάτω τα βήματα για Anthropic (προτεινόμενο, ήδη
δοκιμασμένο σε όλη την ανάπτυξη).

---

## 1) Δημιουργία λογαριασμού + key

1. Πήγαινε στο **https://console.anthropic.com**
2. Κάνε εγγραφή (ή σύνδεση αν το ΕΚΠΑ έχει ήδη λογαριασμό)
3. **Settings → API Keys → Create Key**
4. Δώσε ένα περιγραφικό όνομα (π.χ. `ekpa-smart-finder-production`)
5. Αντέγραψε το key (ξεκινάει με `sk-ant-...`) — **δεν ξαναεμφανίζεται**, αν το
   χάσεις πρέπει να φτιάξεις νέο

## 2) Φόρτωση credit

Το console.anthropic.com λειτουργεί με prepaid credit (Billing → Add Credit). Δεν
υπάρχει δωρεάν tier με απεριόριστη χρήση — χρειάζεται μια αρχική κατάθεση (ακόμα
και μικρή, π.χ. $5-10, αρκεί για δοκιμές).

## 3) Ενδεικτικό κόστος

Το `/api/chat` χρησιμοποιεί το μοντέλο `claude-sonnet-4-6` (ορίζεται στο
`.env.example` ως `ANTHROPIC_MODEL`). Το κόστος χρεώνεται ανά token (λέξη/κομμάτι
λέξης) εισόδου+εξόδου. Το backend περιορίζει ήδη το μέγεθος του context (μόνο τα
πιο σχετικά προγράμματα ανά ερώτημα, όχι όλος ο κατάλογος), οπότε το κόστος ανά
ερώτημα είναι μικρό. Για ακριβή, ενημερωμένη τιμολόγηση δες
**https://www.anthropic.com/pricing** πριν αποφασίσετε budget — οι τιμές μπορεί να
έχουν αλλάξει από τη στιγμή που γράφτηκε αυτό το έγγραφο.

## 4) Rate limiting (προστασία από απρόσμενο κόστος)

Το backend έχει ήδη ενσωματωμένο rate limiting ώστε να μην μπορεί κανείς (ή bug)
να "κάψει" το budget:

| Env var | Default | Τι κάνει |
|---|---|---|
| `CHAT_RATE_LIMIT_PER_MIN` | 20 | Μέγιστες κλήσεις `/api/chat` ανά IP ανά λεπτό |
| `SEARCH_RATE_LIMIT_PER_MIN` | 120 | Μέγιστες κλήσεις `/api/search` ανά IP ανά λεπτό (δωρεάν, δεν καλεί LLM) |

Μπορείτε να τα κατεβάσετε ακόμα πιο χαμηλά αν θέλετε επιπλέον ασφάλεια, χωρίς
αλλαγή κώδικα — απλά αλλαγή στο `.env` και restart.

Επιπλέον, κάθε αίτημα chat έχει σκληρά όρια μεγέθους (ώστε κανείς να μη μπορεί να
στείλει τεράστιο κείμενο που θα χρεωθεί): μήνυμα έως 700 χαρακτήρες, ιστορικό έως
10 μηνύματα / 8.000 χαρακτήρες συνολικά, συνολικό αίτημα έως 64KB.

## 5) Βάλε το key στο `.env`

```
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-το-key-σου
```

**Ποτέ** μην το βάλεις σε αρχείο που πάει σε git (το `.env` είναι ήδη στο
`.gitignore`), ποτέ στον browser/frontend κώδικα, ποτέ σε screenshot που θα
μοιραστεί δημόσια.

## 6) Αν θέλεις να δοκιμάσεις εναλλακτικό provider (OpenAI ή Gemini)

Αλλάζεις μόνο το `.env`, καμία αλλαγή κώδικα:
```
LLM_PROVIDER=openai
OPENAI_API_KEY=...
```
ή
```
LLM_PROVIDER=gemini
GEMINI_API_KEY=...
```
Restart του server μετά την αλλαγή (`pm2 restart ekpa-smart-finder`).
