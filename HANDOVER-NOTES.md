# Handover Notes — EKPA Smart Finder

**Ημερομηνία:** 15 Σεπτεμβρίου 2026 (πακέτο v4 — αρχική έκδοση 7/9/2026)
**Κατάσταση:** Production-ready, έτοιμο για μεταφορά σε δικό σας server (δες
`DEPLOY-SelfHosted.md`). **Το live Railway τρέχει ακόμα v3** μέχρι να γίνει deploy
αυτής της έκδοσης — δες "Αλλαγές v4" παρακάτω.

---

## Τι δουλεύει αυτή τη στιγμή

- Αναζήτηση σε 702 προγράμματα, real-time, ~5-50ms ανά ερώτημα (και στο `/api/search`
  που χρησιμοποιεί το widget του site — βλ. Αλλαγές v4)
- AI βοηθός (floating 💬 widget) μέσω Anthropic API, retrieval-augmented (απαντάει
  μόνο με βάση τον πραγματικό κατάλογο)
- GTM widget στο elearningekpa.gr με live GA4 tracking (search, zero-results,
  clicks)
- Admin panel για επεξεργασία taxonomy χωρίς redeploy
- CORS whitelist + rate limiting σε όλα τα endpoints
- Query length cap (CPU-DoS protection)

## Ανοιχτά θέματα / αποφάσεις που εκκρεμούν

1. **GA4 event name: `search` vs `view_search_results`** — δες `GTMREADME.md`
   §5 για πλήρη ανάλυση. Τρέχουσα προεπιλογή: παραμονή σε `search` (πιο ακριβές
   σημασιολογικά), αλλά είναι μονόγραμμη αλλαγή αν προτιμηθεί το built-in GA4
   report.

2. **Click-based ranking — Φάση 1 έγινε, Φάση 2 ανοιχτή.** Υπάρχει πλέον πλήρες
   σύστημα καταγραφής (§Analytics tab στο `ADMIN-PANEL-GUIDE.md`): ποιες
   αναζητήσεις γίνονται, ποιες δεν φέρνουν αποτέλεσμα, ποια προγράμματα
   κλικάρονται τελικά. Αποθηκεύεται σε `server/analytics.json` (JSON αρχείο, όχι
   database — βλ. σχεδιαστική απόφαση παρακάτω). **Δεν** επηρεάζει ακόμα το
   πραγματικό ranking του `/api/search` — αυτό είναι σκόπιμο (Φάση 2, εκκρεμεί):
   πρώτα μαζεύουμε μερικές εβδομάδες πραγματικά δεδομένα, μετά αποφασίζουμε αν/πώς
   θα μπει ένα capped, log-scaled popularity boost στο σκορ, ώστε να μην μπορεί
   ένα δημοφιλές αλλά άσχετο πρόγραμμα να ξεπεράσει κάτι πιο σχετικό.

3. **PROGRAMS array duplication** — το `public/programs.json` (4.9MB) φορτώνεται
   στη μνήμη τόσο από το `server.js` όσο και σερβίρεται στατικά· καμία πρακτική
   επίπτωση σήμερα (702 προγράμματα είναι μικρό dataset), αλλά αν ο κατάλογος
   μεγαλώσει σημαντικά (π.χ. 5.000+ προγράμματα) θα άξιζε μετάβαση σε πραγματική
   database με indexed search αντί για in-memory JS scoring.

4. **Δεδομένα προγραμμάτων: manual sync.** Το `programs.json` ενημερώνεται
   χειροκίνητα (scrape/export από το elearningekpa.gr), όχι αυτόματα. Αν το CMS
   αλλάξει προγράμματα/τιμές συχνά, θα χρειαστεί μελλοντικά κάποιο automation
   (π.χ. scheduled job) αντί για χειροκίνητο re-export.

5. **Mobile-first redesign** — το layout είναι responsive αλλά σχεδιάστηκε
   desktop-first. Λειτουργεί σε κινητό, αλλά δεν έχει βελτιστοποιηθεί ειδικά γι'
   αυτό.

## Αλλαγές v4 (15 Σεπτεμβρίου 2026)

Αποτέλεσμα πλήρους code review του v3. Όλες ελέγχθηκαν τοπικά (server, index.html,
admin panel, GTM widget σε ξεχωριστό origin, και με jQuery Typeahead).

**Κρίσιμα**
- **`/api/search` ήταν 0.8–2.6 δευτ. ανά αίτημα.** Η διόρθωση ταχύτητας της 7/9 είχε
  μπει μόνο στο αντίγραφο της μηχανής μέσα στο `index.html`, όχι στο κοινό
  `search-engine.js` που χρησιμοποιούν το `/api/search` (δηλ. το widget του
  elearningekpa.gr) και το `/api/chat`. Λίγοι ταυτόχρονοι χρήστες μπλόκαραν τον server
  (και το `/health`). Τώρα: 5–50ms, ίδια ακριβώς top-10 αποτελέσματα σε 34 δοκιμαστικά
  queries. Το `index.html` φορτώνει πλέον το ίδιο `search-engine.js` — δεν υπάρχει
  δεύτερο αντίγραφο.
- **Chat: απεριόριστο ιστορικό.** Το `history` δεν ελεγχόταν (έως 1MB ανά αίτημα →
  δυνητικά τεράστιο κόστος LLM). Τώρα: έως 10 μηνύματα / 8.000 χαρ., αίτημα έως 64KB,
  έλεγχος ρόλων.
- **Self-hosted: το chat θα έβγαζε 403.** Το CORS επέτρεπε τις σελίδες του ίδιου του
  backend μόνο στο Railway. Τώρα οι same-origin σελίδες επιτρέπονται παντού.
- **Click tracking χαλασμένο για ~1/3 του καταλόγου.** 223/702 προγράμματα έχουν
  `id: null`· τα κλικ τους δεν καταγράφονταν (widget) ή πήγαιναν σε λάθος πρόγραμμα
  (index.html). Τώρα το tracking γίνεται με `slug` (υπάρχει πάντα, μοναδικό). Παλιές
  εγγραφές με αριθμητικό id εξακολουθούν να εμφανίζονται σωστά.

**Σοβαρά**
- **Σύντομα queries ("ai", "hr", "it", "bi", "seo") ταίριαζαν σε 50–95% του καταλόγου**
  (π.χ. "hr" → greeklish "ιρ" μέσα στη λέξη "εργαστήριο"). Τώρα: χωρίς greeklish για
  queries < 4 γραμμάτων, και ταίριασμα ως αρχή λέξης για queries ≤ 3 χαρακτήρων
  ("ψυ" → "ψυχολογία" εξακολουθεί να δουλεύει). "ai": 667 → 32 αποτελέσματα.
- **Χάνονταν οι αλλαγές του admin και τα analytics σε κάθε Railway redeploy** (και
  συγκρούονταν με `git pull` σε δικό σας server). Νέα μεταβλητή `DATA_DIR` — δες
  `DEPLOY-Railway.md` §2 / `DEPLOY-SelfHosted.md` §3. Οι εγγραφές αρχείων είναι πλέον
  atomic.
- **Analytics:** τα προθέματα της live αναζήτησης ("ψ", "ψυ", "ψυχ"…) μετρούσαν ως
  ξεχωριστές αναζητήσεις / ψεύτικα zero-results — τώρα ενώνονται σε μία. Το όριο
  αυξήθηκε 300 → 2000 και η αφαίρεση προτιμά τα παλιά σπάνια queries (πριν, κάθε νέο
  query έσβηνε το προηγούμενο νέο).
- **GTM widget:** debounce και στη διαδρομή Typeahead (πριν: αίτημα σε κάθε πλήκτρο)·
  αγνοούνται απαντήσεις που φτάνουν εκτός σειράς· click tracking και
  `search_no_results` και στη διαδρομή Typeahead· beacon ως `text/plain` (χωρίς CORS
  preflight)· αν δεν υπάρχει `gtag()`, τα GA4 events πάνε στο `dataLayer` αντί να
  χάνονται (`GTMREADME.md` §6.1)· παράκαμψη γνωστού bug του typeahead.js 0.11.1
  (async αποτελέσματα δεν εμφανίζονταν όταν πλήθος = limit).
- **gzip:** το `/api/programs` πέφτει από 4.9MB σε ~0.8MB.

**Μικρότερα**
- Το chat φέρνει στο context και προγράμματα από το προηγούμενο ερώτημα του χρήστη
  (ερωτήσεις συνέχειας όπως "και πόσο κοστίζει;").
- Τα σφάλματα του LLM provider δεν εμφανίζονται πια στον χρήστη (μόνο στο log).
- Σύγκριση `ADMIN_TOKEN` σε σταθερό χρόνο· Gemini key σε header αντί για URL.
- Αφαιρέθηκε νεκρός κώδικας στο `index.html` (system prompt που χτιζόταν στον browser
  αλλά δεν χρησιμοποιούνταν ποτέ).
- Διορθώσεις στα έγγραφα (παλιές οδηγίες για `BACKEND_URL`, λείπον `ADMIN_TOKEN`
  στο Railway, αντιφάσεις για analytics).

**Deploy της v4:** το `package.json` έχει νέα εξάρτηση (`compression`) → χρειάζεται
`npm install`. Ορίστε `DATA_DIR` **πριν** το deploy· στο πρώτο ξεκίνημα θα αντιγραφεί
το `public/concepts.json`. ⚠️ Αν έχουν γίνει αλλαγές taxonomy από το admin panel στο
τρέχον Railway, κατεβάστε πρώτα το live `https://…/concepts.json` και βάλτε το στο
`public/concepts.json`, αλλιώς θα χαθούν. Το GTM Custom HTML tag πρέπει να
ενημερωθεί με το νέο `ekpa-search-widget-gtm-v2.js` → Publish.

## Συγχώνευση παράλληλων fixes στο live repo (15 Σεπτεμβρίου 2026, αργότερα την ίδια μέρα)

Ενόσω γινόταν το v4 rewrite σε αυτή τη sessions, ο χρήστης έκανε **ξεχωριστά, τοπικά,
5 commits** πάνω στο ήδη-live repo (`git log`: `331643e`, `c11d556`, `03fdbcd`,
`6ed781d`, `5d2bf0c`) — αυτά **δεν** ήταν μέρος του uploaded zip πάνω στο οποίο
χτίστηκε το v4, οπότε το πρώτο v4 delivery δεν τα περιείχε. Έγιναν όλα reconciled
πάνω στην αρχιτεκτονική v4 (κοινό `search-engine.js`, χωρίς duplication). Λεπτομέρειες:

- **Αφαίρεση placeholder/chips από το search box** (`331643e`) — καθαρά UI, ported ως έχει.
- **Σειρά dropdown κατηγοριών να ταιριάζει με το μενού του elearningekpa.gr**
  (`c11d556`) — νέος πίνακας `SITE_CATEGORY_ORDER` στο `index.html`, ό,τι δεν
  υπάρχει σε αυτόν πέφτει αλφαβητικά στο τέλος.
- **Greeklish "naftiliaka" → "ναυτιλιακά"** (`03fdbcd`) — το "αυ"/"ευ" προφέρεται
  (και γράφεται σε greeklish) "af/av"/"ef/ev" ανάλογα με το επόμενο σύμφωνο· νέα
  `transliterateGreeklishAuEu()` δοκιμάζει και τις δύο αναγνώσεις όταν το μοτίβο
  εμφανίζεται. **Στένεψε και η ανοχή τυπογραφικών λαθών** (`sharesPrefix`): πριν
  αρκούσαν τα ίδια πρώτα 3 γράμματα ανεξαρτήτως μήκους (άνοιγε ψευδή matches όπως
  "διοικηση" ↔ άσχετες λέξεις μέσω του fuzzy vocabulary)· τώρα απαιτείται ή ίδιο
  μήκος + κοινό prefix, ή διαφορά ενός διπλασιασμένου γράμματος (π.χ. "αγγλικα" vs
  "αγλικα"). Effect: "διοικηση" δεν βγάζει πια το άσχετο "digital leadership" στην
  κορυφή.
- **Stopword "πρόγραμμα/προγράμματα/program/programs"** (`6ed781d`) — κάθε πρόγραμμα
  ΕΙΝΑΙ ένα "πρόγραμμα", η λέξη δεν έχει διακριτική αξία και πνιγε πραγματικά matches.
- **IPSAS/μοριοδότηση cross-contamination** (`6ed781d`) — το concept
  `public_sector_certified` περιείχε "μοριοδοτούμενο"/"μοριοδότηση", που εμφανίζονταν
  και σε 3 τελείως άσχετα προγράμματα (λογιστική/HR/φιλοσοφία, id 821/1094/1306) απλά
  ως ένδειξη επιλεξιμότητας δημοσίων υπαλλήλων — αυτό τα έκανε να ανταγωνίζονται τα
  πραγματικά μοριοδοτούμενα προγράμματα παιδαγωγικών για το query "μοριοδοτούμενο".
  Αφαιρέθηκαν οι δύο όροι από το concept και τα tags των 3 προγραμμάτων.
- **Νέο concept `industrial_manufacturing`** (βιομηχανική/industrial/manufacturing) (`6ed781d`).
- **Coverage bonus** (`5d2bf0c`, το feature που είχε ήδη αναφερθεί ξεχωριστά στον
  χρήστη) — ανταμείβει προγράμματα που καλύπτουν 2+ διακριτές έννοιες ενός query
  (π.χ. "αθλητική ψυχολογία"). **Βρέθηκε και διορθώθηκε bug** στην αρχική υλοποίηση:
  το "coverage" μετρούσε match ακόμα και μέσα στην πλήρη περιγραφή (`all`), κι έτσι
  ένα άσχετο πρόγραμμα (μυοπεριτονιακή απελευθέρωση) έβγαινε #1 για "αθλητική
  ψυχολογία" επειδή η λίστα προϋποθέσεων εισδοχής ανέφερε παρεμπιπτόντως "αθλητικής
  επιστήμης". Διορθώθηκε ώστε το coverage να μετράει μόνο τίτλο/tags/κατηγορία.
  Επιβεβαιώθηκε με πλήρες regression σε 39 test queries — μηδέν ανεπιθύμητες αλλαγές.

Όλα τα παραπάνω έχουν πλέον ενσωματωθεί ΜΕΣΑ στην αρχιτεκτονική v4 (index.html
φορτώνει το κοινό search-engine.js, καμία δεύτερη κόπια λογικής). Αν στο μέλλον
γίνουν ξανά τοπικά commits παράλληλα με δουλειά σε αυτό το session, ελέγξτε πρώτα το
`git log` του live repo πριν συγχωνεύσετε — αλλιώς αντικαθίστανται σιωπηλά.

## Γνωστά θέματα δεδομένων (δεν αλλάχθηκαν — θέλουν απόφαση/διόρθωση στην πηγή)

- **223 προγράμματα με `id: null`** και **359 αναφορές** στο `similar_program_ids`
  προς id που δεν υπάρχουν στον κατάλογο (τα "Σχετικά" προγράμματα εμφανίζονται μόνο
  όπου το id υπάρχει). Διόρθωση: στο export/scrape του `programs.json`.
- **Λάθος tags από τον αυτόματο εμπλουτισμό**, π.χ. το "Διαταραχές Πρόσληψης Τροφής"
  πήρε tags HR (`hr`, `recruitment`, `προσληψεις`) λόγω της λέξης "Πρόσληψη"· παρόμοια
  στα "Δημιουργική Γραφή: Εργαστήριο Ποιητικού Λόγου", "Θέατρο και Εκπαίδευση…",
  "Ασφάλεια CBRNE/HAZMAT…". Χρειάζεται έλεγχος του offline enrichment script.
- 262 προγράμματα χωρίς τιμή, 112 χωρίς περιγραφή.

## Πρόσφατα διορθωμένα (7–9 Σεπτεμβρίου 2026)

Για ιστορικό, τα εξής διορθώθηκαν στο live repo την ίδια μέρα με αυτό το
handover — αν κάτι φαίνεται περίεργο σε παλιότερα screenshots/notes, γι' αυτό:

- Το live production είχε ξεμείνει σε παλιότερη έκδοση: 497 προγράμματα (αντί
  702), ανοιχτό CORS (`cors()` χωρίς whitelist — οποιοδήποτε site μπορούσε να
  καλέσει το API), καθόλου rate limiting, hardcoded 3.7MB frontend με sidebar
  chat. Όλα αυτά διορθώθηκαν με πλήρες sync του repo.
- Search input lag: η μηχανή αναζήτησης υπολόγιζε το fuzzy typo-matching
  (Levenshtein) ξανά για κάθε ένα από τα 702 προγράμματα σε κάθε πάτημα
  πλήκτρου, αντί μία φορά συνολικά — προκαλούσε 3-5 δευτερόλεπτα καθυστέρηση ανά
  keystroke. Διορθώθηκε: query expansion υπολογίζεται τώρα μία φορά ανά
  αναζήτηση (~200x ταχύτερο).
- **Bug από το παραπάνω fix:** ο AI βοηθός (`/api/chat`) έσπασε προσωρινά μετά
  το performance fix, γιατί μια δεύτερη συνάρτηση (`retrieveCandidates`, μέσα
  στο chat widget) δεν είχε ενημερωθεί με το νέο interface της `score()`.
  Διορθώθηκε την ίδια μέρα, πριν προλάβει να γίνει αντιληπτό από πραγματικούς
  χρήστες σε μεγάλη κλίμακα.
- Προστέθηκε Analytics Φάση 1 (βλ. σημείο 2 παραπάνω) — καταγραφή
  αναζητήσεων/κλικ, ορατό μέσα από νέο tab στο `admin-taxonomy.html`.
- **Search relevance fixes (συστηματική σάρωση 9/9/2026):** εντοπίστηκαν και
  διορθώθηκαν 3 ξεχωριστά bugs όπου η ελληνική φωνητική σύμπτυξη
  (`foldGreek()` — π.χ. οι/υ/η→ι, ω→ο, φτιαγμένη για να πιάνει ορθογραφικές
  παραλλαγές) δημιουργούσε ψεύτικες ομοιότητες ανάμεσα σε εντελώς άσχετες
  λέξεις που τυχαίνει να μοιράζονται κατάληξη μετά τη σύμπτυξη:
  - `ψυχολογία` ↔ `οινολογία`/`μυθολογία` (typo-correction path) — διορθώθηκε
    με απαίτηση κοινού προθέματος πριν δεχτεί δύο λέξεις σαν typo η μία της
    άλλης (`MIN_SHARED_PREFIX`)
  - `αμπελουργία` → `ακτινολογία` (concept-expansion path) — διορθώθηκε με
    απαίτηση 75% επικάλυψης μήκους ανάμεσα σε έναν όρο και τη λέξη μέσα στην
    οποία βρίσκεται (`MIN_TERM_TOKEN_OVERLAP`)
  - `όργανο` ↔ `οργάνωση` (raw-query substring path) — μερικώς μετριασμένο
    (concepts.json fix), αλλά **δεν** διορθώθηκε πλήρως: ο μηχανισμός που
    δίνει βαθμούς όταν ο ίδιος ο όρος αναζήτησης εμφανίζεται ως substring
    μέσα σε τίτλο δεν έχει κανέναν ratio-έλεγχο (θα έσπαγε τη φυσιολογική
    μερική-λέξη αναζήτηση, π.χ. "market"→"marketing"). Τεκμηριωμένο, χαμηλού
    ρίσκου known limitation — κανένα πρόγραμμα του καταλόγου δεν αφορά
    μουσικά όργανα σήμερα, άρα μηδενική πρακτική επίπτωση.
- **Tags enrichment:** το `programs.json` εμπλουτίστηκε αυτόματα (offline
  script, matching μόνο σε τίτλο+κατηγορία — ΟΧΙ σε πλήρη περιγραφή, γιατί
  εκεί δοκιμάστηκε πρώτα και προέκυψε θόρυβος από παραδείγματα εφαρμογών/
  βιογραφικά καθηγητών). Μέσος όρος tags: 18.6 → 20.8.
- Προστέθηκε ορατό relevance score badge σε κάθε κάρτα αποτελέσματος (μόνο
  όταν υπάρχει ενεργό query).

## Επικοινωνία

**Αρχικός developer:** Antonis Papachrisanthou
**Repo:** https://github.com/antoniosxiaomi8pro-source/search-ekpa
**Τρέχον production (Railway, μέχρι να μεταφερθεί):** nodejs-production-5371.up.railway.app

## Σειρά ανάγνωσης για νέο μέλος ομάδας IT

1. `ARCHITECTURE.md` — πώς κουμπώνουν όλα μαζί
2. `DEPLOY-SelfHosted.md` — πώς να το ανεβάσεις στο δικό σου server
3. `API-KEY-SETUP.md` — πώς να πάρεις το δικό σου API key
4. `ADMIN-PANEL-GUIDE.md` — πώς να διαχειρίζεσαι το taxonomy
5. `GTMREADME.md` — πώς δουλεύει το tracking κομμάτι
6. `README.md` (μέσα στο repo) — τεχνικές λεπτομέρειες κώδικα/endpoints
